import React from "react";
import { MealProps } from "../interfaces/MealProps";

export class MealSchedule extends React.Component<MealProps> {
    constructor(props: MealProps) {
        super(props);
    }

    render() {
        return (<div>schedule goes here</div>);
    }
}